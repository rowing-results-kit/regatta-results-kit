# 手動スクショ撮影ガイド — 非エンジニア向け

> 撮ったら `docs/assets/img/onboarding/` に同名で置いて「撮った」と言えば自動で埋め込みます。

**所要時間の目安**: 10分  
**推奨ウィンドウ幅**: 1280px（横長UIが収まる幅）  
**保存形式**: PNG（Macは `Command+Shift+4` → 範囲指定でスクショ）

---

## 優先 Top 3（まずここだけ撮れれば最低限OK）

### ① PAT 作成画面 ＋ 生成画面（最大の難所）

GitHubの認証トークンを発行する画面。**トークン本体が出る唯一の瞬間**なので要注意。

- ファイル1: `gh-pat-create-form.png`（入力フォーム）
- ファイル2: `gh-pat-generated.png`（生成直後・**トークン本体は必ず黒塗り**）
- 到達方法 → [Step 6 PAT作成フォーム](#step6-pat-create) を参照

### ② Cloudflare ビルド設定（Build output = site）

`site` と入力する箇所が一番つまずきやすい設定項目。

- ファイル: `cf-pages-build-settings.png`
- 到達方法 → [Step 2 ビルド設定](#step2-build-settings) を参照

### ③ 管理者ポータル 完了状態（全テスト緑）

セットアップが成功した最終ゴール画面。オンボーディングのゴールイメージになる。

- ファイル: `admin-portal-complete.png`
- 到達方法 → [Step 7 管理者ポータル](#step7-admin-portal) を参照

---

## 全12画面 詳細手順

### Step 2: Cloudflare Pages

---

#### <a id="step2-connect"></a>`cf-pages-create.png` — Pages 作成画面（Connect to Git）

| 項目 | 内容 |
|---|---|
| **どの画面か** | Cloudflare Pages でリポジトリを接続するフォーム |
| **到達方法** | `dash.cloudflare.com` にログイン → 左メニュー「Workers & Pages」→「Pages」→「Create」→「Connect to Git」 |
| **強調する要素** | 「Connect to Git」ボタン・リポジトリ選択ドロップダウン |
| **⚠️ 写してはいけないもの** | 他の個人プロジェクト名・会社名・メールアドレス |
| **推奨幅** | 1280px |

---

#### <a id="step2-build-settings"></a>`cf-pages-build-settings.png` — ビルド設定（Build output directory = site）

| 項目 | 内容 |
|---|---|
| **どの画面か** | Cloudflare Pages のビルドコマンド・出力ディレクトリ設定フォーム |
| **到達方法** | Pages 作成フロー → リポジトリ接続後 → 「Begin setup」→ 設定フォームが表示される |
| **強調する要素** | 「Build output directory」欄に `site` と入力した状態（この値が肝） |
| **⚠️ 写してはいけないもの** | APIキー・環境変数の値・他のプロジェクト情報 |
| **推奨幅** | 1280px |

---

### Step 3: GitHub Actions

---

#### `gh-actions-setup-workflow.png` — Actions サイドバー「大会初期構築」

| 項目 | 内容 |
|---|---|
| **どの画面か** | Actions タブの左サイドバーにワークフロー一覧が出ている画面 |
| **到達方法** | 自分のリポジトリ → タブ「Actions」→ 左サイドバーに「大会初期構築」が表示された状態 |
| **強調する要素** | 左サイドバーの「大会初期構築」の選択状態 |
| **⚠️ 写してはいけないもの** | 他の非公開リポジトリ名・個人メール |
| **推奨幅** | 1280px |

---

#### `gh-actions-run-workflow.png` — Run workflow フォーム（大会名・日付入力）

| 項目 | 内容 |
|---|---|
| **どの画面か** | 「Run workflow」ドロップダウンが開いて入力欄が見えている画面 |
| **到達方法** | Actions → 左サイドバー「大会初期構築」→ 右上「Run workflow」ボタンをクリック → フォームが展開 |
| **強調する要素** | 大会名・開催日・テーマカラーの入力欄（ダミーでも何か入力した状態が望ましい） |
| **⚠️ 写してはいけないもの** | 実在選手データ・個人メール・他のブランチ情報 |
| **推奨幅** | 1280px |

---

#### `gh-actions-error-log.png` — Actions ジョブ失敗ログ

| 項目 | 内容 |
|---|---|
| **どの画面か** | ジョブが失敗した時の赤バツ表示とログ展開画面 |
| **到達方法** | Actions → 赤バツのジョブをクリック → 「scaffold実行」など失敗ステップをクリックしてログを展開 |
| **強調する要素** | 赤バツアイコン・エラーメッセージが見える状態のログ行 |
| **⚠️ 写してはいけないもの** | 秘密情報が流れ込んでいるログ行・PAT本体・環境変数の値 |
| **補足** | 意図的に失敗させる必要はない。過去の失敗ジョブが残っていればそれを使う |
| **推奨幅** | 1280px |

---

### Step 4: Google Drive

---

#### `google-drive-folder-id.png` — Drive フォルダのURL（フォルダIDが見える状態）

| 項目 | 内容 |
|---|---|
| **どの画面か** | Google Drive でフォルダを開いた時のブラウザアドレスバー。URL末尾がフォルダID |
| **到達方法** | `drive.google.com` にログイン → 大会用フォルダを開く → アドレスバーにフォーカス（URLが選択された状態） |
| **強調する要素** | URLの末尾部分（`/folders/XXXXXXXXX` の `XXXXXXXXX` がフォルダID） |
| **⚠️ 写してはいけないもの** | 個人メールアドレス・他のフォルダ内のファイル名・実在選手データ |
| **補足** | ダミーフォルダ（例: 「テスト大会2026」）を作って撮ると安全 |
| **推奨幅** | 1280px |

---

### Step 5: Google Apps Script

---

#### `gas-editor-copy.png` — GAS「ファイル → コピーを作成」

| 項目 | 内容 |
|---|---|
| **どの画面か** | GASエディタのメニューバーから「ファイル → コピーを作成」をクリックしている瞬間 |
| **到達方法** | 以下のURLでテンプレートを開く → メニュー「ファイル」→「コピーを作成」にカーソルを当てた状態でスクショ |
| **テンプレートURL（GAS-A）** | `https://script.google.com/d/1x6sLT71JSl1dmDUQgUGSd_rGMhWWYFPtrcH-gSrAMJCvxapV5a409JBJ/edit` |
| **開発アカウント** | rowing.ishikawa.dev でログイン |
| **強調する要素** | 「コピーを作成」メニュー項目が選択されている状態 |
| **⚠️ 写してはいけないもの** | 個人メール・スクリプト本体のコード（エディタ右ペインは最小化OK） |
| **推奨幅** | 1280px |

---

#### `gas-script-properties.png` — GAS スクリプトプロパティ設定画面

| 項目 | 内容 |
|---|---|
| **どの画面か** | GASプロジェクト設定の「スクリプトプロパティ」セクションが開いた画面 |
| **到達方法** | GASエディタ → 左サイドバー歯車アイコン（プロジェクトの設定）→ 下にスクロール → 「スクリプトプロパティ」セクション → 「スクリプトプロパティを追加」ボタンが見える状態 |
| **テンプレートURL（GAS-A）** | 上記と同じ |
| **強調する要素** | 「スクリプトプロパティを追加」ボタン・プロパティ名/値の入力欄 |
| **⚠️ 写してはいけないもの** | すでに設定済みのプロパティの**値**（APIキー・トークン等）・個人メール |
| **補足** | プロパティ名（キー）だけ見える状態は問題ない。値は必ずマスク/空欄状態で撮る |
| **推奨幅** | 1280px |

---

### Step 6: GitHub PAT（Personal Access Token）

---

#### <a id="step6-pat-create"></a>`gh-pat-create-form.png` — PAT Fine-grained 作成フォーム

| 項目 | 内容 |
|---|---|
| **どの画面か** | GitHub の Fine-grained PAT 作成フォーム（名前・有効期限・権限を設定する画面） |
| **到達方法** | `github.com/settings/personal-access-tokens/new` に直接アクセス（要ログイン） |
| **強調する要素** | Token name 入力欄・Expiration（有効期限）選択・Repository access・Permissions のセクション |
| **⚠️ 写してはいけないもの** | 個人メール・他のPAT一覧 |
| **推奨幅** | 1280px |

---

#### `gh-pat-generated.png` — PAT 生成直後のコピー画面

| 項目 | 内容 |
|---|---|
| **どの画面か** | 「Generate token」を押した直後に一度だけ表示されるトークン文字列の画面 |
| **到達方法** | 上記フォームで「Generate token」をクリック → トークンが表示された画面 |
| **強調する要素** | 「トークンが1回しか表示されない」注意書き・コピーボタン |
| **⚠️⚠️ 写してはいけないもの（最重要）** | **トークン本体（`github_pat_...` で始まる文字列）は必ず黒塗りまたはぼかし処理してから保存**。Macのプレビューアプリで「マークアップ」→「長方形」を使って黒塗りできる |
| **推奨幅** | 1280px |

---

### Step 7: デプロイ ＆ 管理者ポータル

---

#### `gas-deploy-webapp.png` — GAS デプロイ設定（ウェブアプリ）

| 項目 | 内容 |
|---|---|
| **どの画面か** | GAS の「新しいデプロイ」ダイアログで「種類の選択: ウェブアプリ」を選んだ状態 |
| **到達方法** | GASエディタ → 右上「デプロイ」→「新しいデプロイ」→ 歯車アイコン →「ウェブアプリ」を選択 |
| **テンプレートURL（GAS-A）** | `https://script.google.com/d/1x6sLT71JSl1dmDUQgUGSd_rGMhWWYFPtrcH-gSrAMJCvxapV5a409JBJ/edit` |
| **強調する要素** | 「次のユーザーとして実行: 自分」「アクセスできるユーザー: 全員」の設定欄 |
| **⚠️ 写してはいけないもの** | 個人メール・既存デプロイのURLや説明文 |
| **推奨幅** | 1280px |

---

#### <a id="step7-admin-portal"></a>`admin-portal-complete.png` — 管理者ポータル 完了状態

| 項目 | 内容 |
|---|---|
| **どの画面か** | GASウェブアプリの管理者ポータルで全テストが緑（成功）になった状態タブ |
| **到達方法** | GASデプロイ後のウェブアプリURL → 「状態」タブを開く → 全項目が緑チェックになった状態 |
| **強調する要素** | 全ての接続テストが「OK」または緑チェックになっている状態 |
| **⚠️ 写してはいけないもの** | 実在選手データ・個人メール。**サンプル大会状態（ダミーデータ）で撮ること** |
| **補足** | 実在の大会データが入っている場合は事前にサンプルデータに切り替えるか、選手名をぼかす |
| **推奨幅** | 1280px |

---

## 撮影後チェックリスト

- [ ] ファイル名が MANIFEST と完全一致している（例: `gh-pat-generated.png`）
- [ ] `docs/assets/img/onboarding/` フォルダに置いた
- [ ] `gh-pat-generated.png` のトークン部分が黒塗り済み
- [ ] 個人メール・実在選手データが映っていない
- [ ] ウィンドウ幅が1280px程度（横長UIが切れていない）

撮影完了後に「撮った」と伝えると、onboarding/index.html への埋め込みを自動で実行します。

---

## GASテンプレート3本のscriptId一覧（参考）

| 名称 | scriptId |
|---|---|
| GAS-A（メインAPI） | `1x6sLT71JSl1dmDUQgUGSd_rGMhWWYFPtrcH-gSrAMJCvxapV5a409JBJ` |
| GAS-B（スケジュール） | — |
| GAS-C（通知） | — |

GAS-A が主な撮影対象。rowing.ishikawa.dev アカウントでアクセスする。

---

## 更新履歴

| 日付 | 変更内容 |
|---|---|
| 2026-06-13 | 初版作成。非エンジニア向け12画面の手順・警告・優先Top3を整備 |
